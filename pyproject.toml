[project]
name = "samenvoeger"
version = "1.3.0"
description = "PDF-bestanden samenvoegen via een eenvoudige GUI"
requires-python = ">=3.11"
dependencies = [
    "pypdf>=4.0.0",
    "tkinterdnd2>=0.4.2",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
